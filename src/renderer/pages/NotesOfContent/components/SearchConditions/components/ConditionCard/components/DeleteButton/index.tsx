import { VFC } from 'react';

import { DeleteButtonIcon } from './components/DeleteButtonIcon';

export const DeleteButton: VFC = () => {
  return (
    <button>
      <DeleteButtonIcon />
    </button>
  );
};
