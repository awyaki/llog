import { CSSObject } from "@emotion/react";

import { colors } from "~/styleConfig";

export const searchBox: CSSObject = {
  padding: "16px",
  border: `1px solid ${colors.cyan.DEFAULT}`,
  borderRadius: "4px",
};
