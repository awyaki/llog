import { CSSObject } from '@emotion/react';

const container: CSSObject = {
  width: '100%',
  height: '100vh', 
  fontFamily: 'Noto Sans JP, sans-serif',
  position: 'relative',
};


export { container };
